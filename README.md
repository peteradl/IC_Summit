# IC Summit website

Responsive Vite/React rebuild of the IC Summit landing page, prepared for Vercel.

## Run locally

```bash
npm install
npm run dev
```

## Deploy

Import this repository in Vercel. The included `vercel.json` selects the Vite build and `dist` output automatically.
